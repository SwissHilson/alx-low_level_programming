#include <stdio.h>

/**
 * main - Program that prints all the single digit number of base 10
 * Return: 0(Program ran successfully!!)
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	putchar(num);
	putchar('\n');
	return num;
